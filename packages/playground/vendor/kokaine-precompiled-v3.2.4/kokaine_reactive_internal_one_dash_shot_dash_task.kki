/*--------------------------------------------------------------------------
  Single-cell ownership state for one-shot host tasks.

  Every transition stores its winning state before returning a payload. No
  user callback, disposer, or continuation runs while the cell is claimable.
--------------------------------------------------------------------------*/
module interface kokaine/reactive/internal/one-shot-task
 
//------------------------------
//#kki: import declarations
 
import std/core/types = std/core/types pub = "";
import std/core/hnd = std/core/hnd pub = "";
import std/core/exn = std/core/exn pub = "";
import std/core/bool = std/core/bool pub = "";
import std/core/order = std/core/order pub = "";
import std/core/char = std/core/char pub = "";
import std/core/int = std/core/int pub = "";
import std/core/vector = std/core/vector pub = "";
import std/core/string = std/core/string pub = "";
import std/core/sslice = std/core/sslice pub = "";
import std/core/list = std/core/list pub = "";
import std/core/maybe = std/core/maybe pub = "";
import std/core/maybe2 = std/core/maybe2 pub = "";
import std/core/either = std/core/either pub = "";
import std/core/result = std/core/result pub = "";
import std/core/tuple = std/core/tuple pub = "";
import std/core/lazy = std/core/lazy pub = "";
import std/core/show = std/core/show pub = "";
import std/core/debug = std/core/debug pub = "";
import std/core/delayed = std/core/delayed pub = "";
import std/core/console = std/core/console pub = "";
import std/core = std/core = "";
import std/core/unsafe = std/core/unsafe = "";
 
//------------------------------
//#kki: external imports
 
 
//------------------------------
//#kki: fixity declarations
 
 
//------------------------------
//#kki: local imported aliases
 
 
//------------------------------
//#kki: type declarations
 
pub value{1,0,1} type task-stop-reason[22,1,24,14] {
  pub con Task-canceled[23,3,23,15]{0,0,0} : task-stop-reason = 1;
  pub con Task-retired[24,3,24,14]{0,0,0} : task-stop-reason = 2;
};
pub type task-terminal[26,1,28,50] {
  pub con Task-completed[27,3,27,16]{0,0,0} : task-terminal = 1;
  pub con Task-stopped[28,3,28,14](task-stop-state: task-stop-reason){1,0,1} : (task-stop-state : task-stop-reason) -> task-terminal = 2;
};
type task-state[43,1,54,53] <a,b,c> :: (V, V, V) -> V {
  con Task-state-pending[44,3,44,20](pending-payload: a, pending-disposer: (std/core/types/maybe :: V -> V)<c>){0,3,8} : forall<a,b,c> (pending-payload : a, pending-disposer : (std/core/types/maybe :: V -> V)<c>) -> (task-state :: (V, V, V) -> V)<a,b,c> = 1;
  con Task-state-ready[48,3,48,18](ready-payload: a, ready-result: b, ready-disposer: (std/core/types/maybe :: V -> V)<c>){0,4,8} : forall<a,b,c> (ready-payload : a, ready-result : b, ready-disposer : (std/core/types/maybe :: V -> V)<c>) -> (task-state :: (V, V, V) -> V)<a,b,c> = 2;
  con Task-state-running[53,3,53,20]{0,0,0} : forall<a,b,c> (task-state :: (V, V, V) -> V)<a,b,c> = 3;
  con Task-state-terminal[54,3,54,21](terminal-state: task-terminal){0,1,8} : forall<a,b,c> (terminal-state : task-terminal) -> (task-state :: (V, V, V) -> V)<a,b,c> = 4;
};
pub value{0,1,8} type one-shot-task[56,1,57,58] <a,b,c> :: (V, V, V) -> V {
  con One-shot-task[57,3,57,15](task-cell: (std/core/types/ref :: (H, V) -> V)<(std/core/types/global :: H),(task-state :: (V, V, V) -> V)<a,b,c>>){0,1,8} : forall<a,b,c> (task-cell : (std/core/types/ref :: (H, V) -> V)<(std/core/types/global :: H),(task-state :: (V, V, V) -> V)<a,b,c>>) -> (one-shot-task :: (V, V, V) -> V)<a,b,c> = 1;
};
pub type task-claim[36,1,41,3] <a,b,c> :: (V, V, V) -> V {
  pub con Task-claim[37,3,37,12](claim-payload: a, claim-result: (std/core/types/maybe :: V -> V)<b>, claim-disposer: (std/core/types/maybe :: V -> V)<c>){0,5,8} : forall<a,b,c> (claim-payload : a, claim-result : (std/core/types/maybe :: V -> V)<b>, claim-disposer : (std/core/types/maybe :: V -> V)<c>) -> (task-claim :: (V, V, V) -> V)<a,b,c> = 1;
};
pub type task-phase[30,1,34,52] {
  pub con Task-pending[31,3,31,14]{0,0,0} : task-phase = 1;
  pub con Task-ready[32,3,32,12]{0,0,0} : task-phase = 2;
  pub con Task-running[33,3,33,14]{0,0,0} : task-phase = 3;
  pub con Task-finished[34,3,34,15](task-terminal-state: task-terminal){0,1,8} : (task-terminal-state : task-terminal) -> task-phase = 4;
};
 
//------------------------------
//#kki: declarations
 
fun cell[12,5,12,8] : forall<a> (value : a) -> (std/core/types/ref :: (H, V) -> V)<(std/core/types/global :: H),a>;
fun load[15,5,15,8] : forall<a> (value : (std/core/types/ref :: (H, V) -> V)<(std/core/types/global :: H),a>) -> a;
fun store[18,5,18,9] : forall<a> (target : (std/core/types/ref :: (H, V) -> V)<(std/core/types/global :: H),a>, value : a) -> ();
// Automatically generated. Tests for the `Task-canceled` constructor of the `:task-stop-reason` type.
pub fip fun is-task-canceled[23,3,23,15] : (^ task-stop-reason : task-stop-reason) -> std/core/types/bool;
// Automatically generated. Tests for the `Task-retired` constructor of the `:task-stop-reason` type.
pub fip fun is-task-retired[24,3,24,14] : (^ task-stop-reason : task-stop-reason) -> std/core/types/bool;
// Automatically generated. Tests for the `Task-completed` constructor of the `:task-terminal` type.
pub fip fun is-task-completed[27,3,27,16] : (^ task-terminal : task-terminal) -> std/core/types/bool;
// Automatically generated. Tests for the `Task-stopped` constructor of the `:task-terminal` type.
pub fip fun is-task-stopped[28,3,28,14] : (^ task-terminal : task-terminal) -> std/core/types/bool;
// Automatically generated. Tests for the `Task-pending` constructor of the `:task-phase` type.
pub fip fun is-task-pending[31,3,31,14] : (^ task-phase : task-phase) -> std/core/types/bool;
// Automatically generated. Tests for the `Task-ready` constructor of the `:task-phase` type.
pub fip fun is-task-ready[32,3,32,12] : (^ task-phase : task-phase) -> std/core/types/bool;
// Automatically generated. Tests for the `Task-running` constructor of the `:task-phase` type.
pub fip fun is-task-running[33,3,33,14] : (^ task-phase : task-phase) -> std/core/types/bool;
// Automatically generated. Tests for the `Task-finished` constructor of the `:task-phase` type.
pub fip fun is-task-finished[34,3,34,15] : (^ task-phase : task-phase) -> std/core/types/bool;
// Automatically generated. Retrieves the `claim-payload` constructor field of the `:task-claim` type.
pub fun task-claim/claim-payload[38,5,38,17] : forall<a,b,c> (^ @this : (task-claim :: (V, V, V) -> V)<a,b,c>) -> a;
// Automatically generated. Retrieves the `claim-result` constructor field of the `:task-claim` type.
pub fun task-claim/claim-result[39,5,39,16] : forall<a,b,c> (^ @this : (task-claim :: (V, V, V) -> V)<a,b,c>) -> (std/core/types/maybe :: V -> V)<b>;
// Automatically generated. Retrieves the `claim-disposer` constructor field of the `:task-claim` type.
pub fun task-claim/claim-disposer[40,5,40,18] : forall<a,b,c> (^ @this : (task-claim :: (V, V, V) -> V)<a,b,c>) -> (std/core/types/maybe :: V -> V)<c>;
pub fun task-claim/@copy[37,3,37,12] : forall<a,b,c> (@this : (task-claim :: (V, V, V) -> V)<a,b,c>, claim-payload : (std/core/types/@optional :: V -> V)<a>, claim-result : (std/core/types/@optional :: V -> V)<(std/core/types/maybe :: V -> V)<b>>, claim-disposer : (std/core/types/@optional :: V -> V)<(std/core/types/maybe :: V -> V)<c>>) -> (task-claim :: (V, V, V) -> V)<a,b,c>;
// Automatically generated. Tests for the `Task-state-pending` constructor of the `:task-state` type.
fip fun is-task-state-pending[44,3,44,20] : forall<a,b,c> (^ task-state : (task-state :: (V, V, V) -> V)<a,b,c>) -> std/core/types/bool;
// Automatically generated. Tests for the `Task-state-ready` constructor of the `:task-state` type.
fip fun is-task-state-ready[48,3,48,18] : forall<a,b,c> (^ task-state : (task-state :: (V, V, V) -> V)<a,b,c>) -> std/core/types/bool;
// Automatically generated. Tests for the `Task-state-running` constructor of the `:task-state` type.
fip fun is-task-state-running[53,3,53,20] : forall<a,b,c> (^ task-state : (task-state :: (V, V, V) -> V)<a,b,c>) -> std/core/types/bool;
// Automatically generated. Tests for the `Task-state-terminal` constructor of the `:task-state` type.
fip fun is-task-state-terminal[54,3,54,21] : forall<a,b,c> (^ task-state : (task-state :: (V, V, V) -> V)<a,b,c>) -> std/core/types/bool;
// Automatically generated. Retrieves the `task-cell` constructor field of the `:one-shot-task` type.
fun one-shot-task/task-cell[57,17,57,25] : forall<a,b,c> (^ @this : (one-shot-task :: (V, V, V) -> V)<a,b,c>) -> (std/core/types/ref :: (H, V) -> V)<(std/core/types/global :: H),(task-state :: (V, V, V) -> V)<a,b,c>>;
pub fun one-shot-task/@copy[57,3,57,15] : forall<a,b,c> (@this : (one-shot-task :: (V, V, V) -> V)<a,b,c>, task-cell : (std/core/types/@optional :: V -> V)<(std/core/types/ref :: (H, V) -> V)<(std/core/types/global :: H),(task-state :: (V, V, V) -> V)<a,b,c>>>) -> (one-shot-task :: (V, V, V) -> V)<a,b,c>;
pub fun new-one-shot-task[60,9,60,25] : forall<a,b,c> (payload : b) -> (one-shot-task :: (V, V, V) -> V)<b,c,a>;
pub fun one-shot-task/phase[63,18,63,36] : forall<a,b,c> (task : (one-shot-task :: (V, V, V) -> V)<b,c,a>) -> task-phase;
pub fun one-shot-task/accept[71,9,71,28] : forall<a,b,c> (task : (one-shot-task :: (V, V, V) -> V)<b,c,a>, result : c) -> std/core/types/bool;
// `Nothing` means the disposer was retained by the task. `Just(disposer)`
// transfers it back to setup code because another transition already won.
pub fun one-shot-task/install-disposer[81,9,81,38] : forall<a,b,c> (task : (one-shot-task :: (V, V, V) -> V)<b,c,a>, disposer : a) -> (std/core/types/maybe :: V -> V)<a>;
pub fun one-shot-task/claim-ready[93,9,93,33] : forall<a,b,c> (task : (one-shot-task :: (V, V, V) -> V)<b,c,a>) -> (std/core/types/maybe :: V -> V)<(task-claim :: (V, V, V) -> V)<b,c,a>>;
pub fun one-shot-task/claim-stop[103,9,103,32] : forall<a,b,c> (task : (one-shot-task :: (V, V, V) -> V)<b,c,a>, reason : task-stop-reason) -> (std/core/types/maybe :: V -> V)<(task-claim :: (V, V, V) -> V)<b,c,a>>;
pub fun one-shot-task/finish[116,9,116,28] : forall<a,b,c> (task : (one-shot-task :: (V, V, V) -> V)<b,c,a>) -> std/core/types/bool;
 
fun @kokaine-cache-phase[1,0,1,0] : () -> std/core/types/int;
 
//------------------------------
//#kki: external declarations
 
 
//------------------------------
//#kki: inline definitions
 
//.inline-section
fun @kokaine-cache-phase // inline size: 1
  = fn(){ 0; };
